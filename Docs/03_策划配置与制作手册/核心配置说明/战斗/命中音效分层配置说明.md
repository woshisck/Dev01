---
status: current
type: guide
owner: combat
updated: 2026-08-10
---

# 命中音效分层配置说明

## 作用

解决「武器种类 × 敌人材质」的音效组合爆炸问题。

玩家武器有锤、枪、剑等多种，敌人材质分为 Soft / Hard / Immaterial。如果每个组合都单独做资产，成本是 `武器数 × 材质数 × 冲击等级数`（乘法增长），加一把新武器就要补一整排音效。

本方案改为**两层叠加**：命中瞬间同时播放两个音源，各自负责复合音的不同包络段。

```text
        t=0ms         30ms        150ms                400ms
         |             |            |                    |
武器瞬态  |=====#####===|            |                    |   短、干、无混响尾巴
（攻击方）|                                               |   回答「是什么打的」
         |                                               |
材质本体  |    ####################=================------|   带共振和衰减尾巴
（受击方）|                                               |   回答「打在什么上」
         |                                               |
合成结果  |=====###############=================----------|
```

人耳靠前 30ms 的瞬态判断**声源**，靠尾巴判断**被敲击的物体**，所以这个切分符合听觉习惯，两层叠加会被听成一个完整打击音，而不是两个音同时响。成本变成 `武器数 + 材质数 × 冲击等级数`（加法增长）。

## 配置位置

| 层 | 配置对象 | 路径 |
| --- | --- | --- |
| 武器瞬态（攻击方） | `UWeaponDefinition` | 各武器 DA → `Combat\|Hit Feedback` 分类 |
| 受击反馈（受击方） | `UEnemyHitImpactData` | `Content/Data/Combat/DA_EnemyHitImpact`，由 Project Settings → Game → Yog → Combat 引用 |
| 敌人材质标签 | `UHitImpactVisualComponent` | 敌人蓝图 → `Combat\|Hit Impact Visual\|Hit React` → `HitReactMaterial` |

受击方**只有一张表**。材质、Buff 状态、护甲全部走同一份 `Entries`，用 GameplayTag 匹配 + `Priority` 排序，不存在第二张覆盖表。

## 受击方：Entries 与级联规则

每条 `Entries` 是一条规则：「当受击者身上有 `MatchTag` 时，贡献这份反馈」。

命中时的解析流程：

1. 收集受击者的标签集合 = `HitReactMaterial` + ASC 上所有标签 +（`ArmorHP > 0` 时自动补 `Buff.Status.Armored`）。
2. 取出所有 `MatchTag` 命中的条目，按 `Priority` 从高到低排序。
3. **每个字段各自独立地，从高优先级往下取第一个「填了的」值。**

第 3 条是关键：高优先级条目可以只填 VFX，声音自动继承下层材质条目的。

```text
受击者标签 = { HitReact.Material.Hard, Buff.Status.Poisoned }

  Buff.Status.Poisoned    Priority 50
    Sound: (空)      VFX: NS_PoisonHit
  HitReact.Material.Hard  Priority 10
    Sound: SW_HardHit    VFX: NS_Sparks

  最终结果：Sound = SW_HardHit    （Poisoned 没填，级联到下层）
            VFX   = NS_PoisonHit  （Poisoned 优先级高，胜出）
```

### 字段说明

| 字段 | 说明 |
| --- | --- |
| `MatchTag` | 匹配标签，可填 `HitReact.Material.*` 或任意受击者会持有的状态标签（`Buff.Status.*`）。父标签会匹配所有子标签 |
| `Priority` | 数值越大越优先。相同数值按数组顺序，建议保持数值互不相同 |
| `Levels` | 强度档数组，由攻击方武器的 `HitImpactLevel` 选择（1 起，越界自动 Clamp）。留空则本条只贡献震屏和静音开关 |
| `CameraShakeLevel` | 震屏等级，`0` = 不贡献。取级联中第一个非 0 值 |
| `bSuppressWeaponTransient` | 勾选后压掉攻击方的武器瞬态层。任一命中目标勾选即对整次挥击生效 |

`Levels` 每条包含 `Sound` / `SoundVolume` / `SoundPitch` / `VFX` / `VFXScale` / `VFXRotationOffset`。

## 攻击方：武器瞬态层

| 字段 | 显示名 | 说明 |
| --- | --- | --- |
| `ImpactTransientSound` | 命中瞬态音效 | 该武器的瞬态音源。留空则只播受击方的音 |
| `ImpactTransientVolume` | 命中瞬态音量 | 瞬态层音量 |
| `ImpactTransientPitch` | 命中瞬态音调 | 瞬态层音调 |
| `HitImpactLevel` | 命中冲击等级 | 选择受击方 `Levels` 里第几条（1 起） |

## 震屏（CameraShakeLevel）

### 解析链路

```text
Entries 级联，取第一个非 0 的 CameraShakeLevel
        │
        ▼
每个受击目标各返回一个等级
        │
        ▼
GA_MeleeAttack 取所有目标的最大值（不是累加）
        │
        ▼
AYogPlayerCameraManager::PlayShakeLevel(等级)
        │
        ▼
UYogSettings.CameraShakeLevelTable → DT_CameraShakeLevels 查表
        │
        ▼
FCameraShakeLevelRow.ShakeClass + Scale → 实际播放
```

一次挥击**只震一次**。命中 5 个敌人取其中最高等级，不会叠成 5 次震动。

### DT_CameraShakeLevels 当前内容

| 行名 | Level | ShakeClass |
| --- | --- | --- |
| `HeavyAttack` | 1 | `B_CameraShakeBase` |
| `CriticalHit` | 2 | `B_CameraShake_UltraSword` |

`Entries` 里填的 `CameraShakeLevel` 必须能在这张表里查到对应 `Level`，否则不震。新增等级要先在 `DT_CameraShakeLevels` 加行。

### 重要行为变更

**旧版只有 Buff 行能产生震屏，材质层不能。现在任何条目都能，包括三条材质条目。**

材质条目会命中**每一次**普通攻击，所以：

- 三条 `HitReact.Material.*` 条目的 `CameraShakeLevel` **保持 `0`**，除非确实想让每一次普通命中都震屏。
- 震屏只配在需要强调的状态条目上（护甲弹开、无敌、招架）。
- 忘记这一点最典型的表现是「打小怪也一直在震」。

### 与其他震屏来源的关系

震屏还有另外两个独立来源，配置时注意不要叠加过量：

| 来源 | 配置位置 | 说明 |
| --- | --- | --- |
| 受击方条目 | `DA_EnemyHitImpact.Entries.CameraShakeLevel` | 本文所述，按受击者标签 |
| 攻击表现 Cue | `UHitCueData.CameraShakeLevel` / `CritCameraShakeLevel` | 按攻击动作 / 是否暴击 |
| 全局伤害缩放 | `UYogSettings.HitShakeConfig` | `UHitCueData` 等级为 `0` 时的兜底，按伤害值缩放 |

三者会各自独立触发。同一次命中若三处都配了，玩家会收到多次震动。

## 推荐配置

| 项 | 推荐值 |
| --- | --- |
| 瞬态资产时长 | 30-80ms |
| 瞬态资产处理 | 干声，不带混响尾巴、不带空间感 |
| `ImpactTransientVolume` | `1.0` 起调；压不住材质层时降到 `0.7-0.9` |
| 材质层时长 | 150-400ms，尾巴由本层负责 |
| 材质条目 `Priority` | `10` |
| 状态条目 `Priority` | `50`（中毒、流血等）／`80`（护甲）／`100`（无敌、招架） |
| 材质条目 `CameraShakeLevel` | `0`（必须，否则每次普通命中都震屏） |
| 状态条目 `CameraShakeLevel` | `0` 起；只在护甲弹开一类需要强调的条目上填 `1` |

## 配置示例

武器瞬态（每把武器一个）：剑 = 金属薄刃「呲」；锤 = 低频木质「咚」；枪 = 弹头拍击「啪」。

`DA_EnemyHitImpact.Entries` 最小配置：

| MatchTag | Priority | CameraShakeLevel | 内容 |
| --- | --- | --- | --- |
| `HitReact.Material.Soft` | 10 | 0 | 湿润挤压破裂声 |
| `HitReact.Material.Hard` | 10 | 0 | 带余振金属撞击声 |
| `HitReact.Material.Immaterial` | 10 | 0 | 空气感嗡鸣扩散声 |
| `Buff.Status.Poisoned` | 50 | 0 | 只填中毒 VFX，声音级联继承材质层 |
| `Buff.Status.Bleeding` | 50 | 0 | 只填流血 VFX，声音级联继承材质层 |
| `Buff.Status.Armored` | 80 | 1 | 硬质弹开声，压过材质层，附带震屏 |

剑砍 Soft = 「呲」+ 湿润挤压；锤砸 Hard = 「咚」+ 金属余振。

## 验收方式

1. 打开 `DA_EnemyHitImpact`，确认 `Entries` 可编辑，`MatchTag` 选择器能选到 `HitReact.Material.*` 和 `Buff.Status.*`。
2. 打开 `B_EnemyBase`，确认 `HitReactMaterial` 下拉只列出 `HitReact.Material.*`。
3. PIE 中用两把武器（一把配瞬态、一把留空）打同一个敌人，确认能听出武器差异，留空那把行为不变。
4. PIE 中用同一把武器打不同材质的敌人，确认能听出材质差异。
5. 给敌人挂中毒（`GE_Poison`）后命中，确认中毒 VFX 生效，同时材质声音仍然通过级联播放。
6. 打护甲状态（`ArmorHP > 0`）的敌人，确认走到 `Buff.Status.Armored` 条目。
7. 一次挥击命中 3 个以上敌人，确认武器瞬态**只响一次**，不出现音量突刺或金属抖动感（comb filter）。
8. 连续平砍普通小怪 10 次，确认**完全不震屏**（材质条目 `CameraShakeLevel` 都是 `0`）。这是本次改动最容易踩的回归点。
9. 一次挥击命中 3 个以上带震屏条目的敌人，确认只震**一次**（取最大值），不是每个目标震一次。
10. 在某条目上把 `CameraShakeLevel` 填成 `DT_CameraShakeLevels` 里不存在的等级，确认不震且不报错。
8. 在某条目上勾 `bSuppressWeaponTransient`，命中持有该标签的目标，确认武器瞬态静音而受击方音效仍然播放。

## 注意事项

- **武器瞬态是「每次挥击一次」，不是「每个受击目标一次」。** 由 `GA_MeleeAttack` 在受击循环外播放。一次挥击只让武器响一次；若按目标数叠加，同帧多份相同音源会造成音量突刺和 comb filter，听起来像 bug 而不是更重的打击。
- **`HitImpactLevel` 是武器上的常量**，不随单次命中变化，所以瞬态资产在制作时就已经隐含该武器的强度，不需要再按等级缩放瞬态层。
- **枪的枪响不在这里配。** 枪口爆音在开火时由 `AN_FireProjectile` 在武器位置播放。这里的瞬态是弹头**命中瞬间**的拍击声。字段语义是「该武器命中接触时产生的瞬态」，不是「该武器的声音」。
- **护甲是属性不是标签。** `ArmorHP > 0` 时由 `UHitImpactVisualComponent` 自动补 `Buff.Status.Armored` 到匹配集合，不需要任何 GE 授予该标签。反过来说，如果以后真的有 GE 授予了它，两者会指向同一条目，不会冲突。
- **`Buff.Status.Invulnerable` 当前没有任何来源授予**，配了条目也不会命中。等实际有 GE / FA 授予后再配。
- **后续待开发**：远程命中路径当前不走 `PlayHitFeedback`，枪的命中分层音效尚未接通；敌人攻击玩家时攻击方没有 `UWeaponDefinition`，只有受击方层，没有瞬态层。
- **震屏来源从「只有 Buff 行」扩大到「任何条目」。** 材质条目每次普通命中都会匹配，`CameraShakeLevel` 必须留 `0`，否则打小怪会持续震屏。详见上文「震屏」章节。
- **震屏按目标取最大值，不累加。** 一次挥击只震一次，这是刻意设计：震屏是玩家全局感受，N 个目标不应该变成 N 次震动。
- 旧的 `DT_BuffHitFeedback` 表和 `EHitReactTier` 枚举已移除，配置全部迁移到 `Entries`。
